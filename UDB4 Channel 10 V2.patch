Index: libUDB/libUDB_defines.h
===================================================================
--- libUDB/libUDB_defines.h	(revision 1147)
+++ libUDB/libUDB_defines.h	(working copy)
@@ -210,6 +210,7 @@
 #define CHANNEL_7		7
 #define CHANNEL_8		8
 #define CHANNEL_9		9
+#define CHANNEL_10		10
 
 
 // Constants
Index: libUDB/servoOut.c
===================================================================
--- libUDB/servoOut.c	(revision 1147)
+++ libUDB/servoOut.c	(working copy)
@@ -33,6 +33,7 @@
 #define SERVO_OUT_PIN_7			_LATD6
 #define SERVO_OUT_PIN_8			_LATD7
 #define SERVO_OUT_PIN_9			_LATA4
+#define SERVO_OUT_PIN_10		_LATA1
 
 #define ACTION_OUT_PIN			SERVO_OUT_PIN_9
 
@@ -106,7 +107,8 @@
 	
 #if (BOARD_TYPE == UDB4_BOARD)
 	_TRISD0 = _TRISD1 = _TRISD2 = _TRISD3 = _TRISD4 = _TRISD5 = _TRISD6 = _TRISD7 = 0 ;
-	if (NUM_OUTPUTS >= 9) _TRISA4 = 0 ;	
+	if (NUM_OUTPUTS >= 9)  _TRISA4 = 0 ;	
+	if (NUM_OUTPUTS >= 10) _TRISA1 = 0 ;
 	
 	
 #else // Classic board
@@ -233,7 +235,11 @@
 			HANDLE_SERVO_OUT(9, SERVO_OUT_PIN_9) ;
 			break ;
 		case 9:
-			SERVO_OUT_PIN_9 = 0 ;	// end the pulse by setting the SERVO_OUT_PIN_9 pin low
+			SERVO_OUT_PIN_9 = 0 ;
+			HANDLE_SERVO_OUT(10, SERVO_OUT_PIN_10) ;
+			break ;
+		case 10:
+			SERVO_OUT_PIN_10 = 0 ;	// end the pulse by setting the SERVO_OUT_PIN_10 pin low
 			_T4IE = 0 ;				// disable timer 4 interrupt
 			break ;
 	}
Index: MatrixPilot/config_tests.c
===================================================================
--- MatrixPilot/config_tests.c	(revision 1147)
+++ MatrixPilot/config_tests.c	(working copy)
@@ -101,8 +101,8 @@
 	#endif
 #else
 	// UDB4
-	#if (NUM_OUTPUTS > 9)
-		#error("NUM_OUTPUTS can't be more than 9.")
+	#if (NUM_OUTPUTS > 10)
+		#error("NUM_OUTPUTS can't be more than 10.")
 	#endif
 #endif
 
Index: MatrixPilot/options.h
===================================================================
--- MatrixPilot/options.h	(revision 1147)
+++ MatrixPilot/options.h	(working copy)
@@ -199,7 +199,8 @@
 //   4 also enables E0 as the 4th output channel
 //   5 also enables E2 as the 5th output channel
 //   6 also enables E4 as the 6th output channel
-//   NOTE: If USE_PPM_INPUT is enabled above, up to 9 outputs are available.)
+//   NOTE: If USE_PPM_INPUT is enabled above, up to 9 outputs are available.
+// 			If UDB4 then up to 10 outputs are available
 #define NUM_OUTPUTS							4
 
 // Channel numbers for each output
